<p style="color: green"><%= notice %></p>


<div id="products">
  <div class="container">
    <div class="row">
    <h1>Products</h1>
      <% @products.each do |product| %>
        <%= render product %>
      <% end %>
    </div>
  </div>
</div>

<%= link_to "New product", new_product_path %>
